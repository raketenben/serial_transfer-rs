[package]
name = "serial_transfer"
version = "0.1.0"
edition = "2021"

[dependencies]
serialport = { version = "4.3.0", optional = true }

[dev-dependencies]
serialport = { version = "4.3.0" }

[features]
default = ["serialport_cast"]
serialport_cast = ["serialport"]